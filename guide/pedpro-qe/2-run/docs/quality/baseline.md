# Baseline — ค่า "ก่อน"

> **สถานะ:** ยังไม่ได้วัด — ทุกช่องเป็น `—`
> รัน `./scripts/record-baseline.sh` เพื่อเติมช่องที่เติมอัตโนมัติได้
>
> **⭐ ทำไมต้องบันทึกก่อนแก้อะไร**
> ถ้าไม่มีค่า "ก่อน" คุณจะพิสูจน์ไม่ได้ว่าสิ่งที่ทำมีผล
> และจะไม่มีตัวเลขไปเขียนเนื้อหาตามภาคผนวก M

---

## วิธีใช้ไฟล์นี้

1. รัน `./scripts/record-baseline.sh` — เติมช่องอัตโนมัติ
2. เติมช่องที่ต้องรันด้วยมือ (gate drill / approval drill)
3. **อย่าแก้ค่าเดิม** — เพิ่มแถวใหม่ทุกครั้งที่วัดซ้ำ
4. ทบทวนเดือนละครั้ง

---

## A. สุขภาพของ test suite

| วันที่ | จำนวนเทส | เวลารวม | เทส > 200ms | shuffle พัง | flake rate |
|---|---|---|---|---|---|
| `<วันนี้>` | — | — | — | — | — |

**วิธีวัด:**
```bash
./scripts/phase1-audit.sh          # ได้ 4 ช่องแรก
pnpm test:shuffle                  # ได้ช่อง shuffle
./scripts/nightly-flake.sh         # ได้ flake rate (ต้องรัน 10 รอบ)
```

---

## B. Anti-pattern ที่เหลืออยู่ (ratchet — ห้ามเพิ่ม)

| pattern | ค่าเริ่มต้น | เป้า | ปัจจุบัน |
|---|---|---|---|
| `waitForTimeout` / `sleep` | — | 0 | — |
| `it.only` / `describe.only` | — | 0 | — |
| `.skip` (ไม่มีเหตุผล) | — | 0 | — |
| `toBeDefined()` | — | ลดลง | — |
| `toBeTruthy()` | — | ลดลง | — |
| `v-html` ไม่ sanitize | — | 0 | — |
| `word-break: break-all` | — | 0 | — |
| `100vh` | — | 0 | — |
| `catch(() => {})` | — | 0 | — |

**⭐ ตั้ง ratchet ทันทีที่วัดได้ครั้งแรก** — ตัวเลขห้ามเพิ่มขึ้นจากวันนั้น

---

## C. Gate drill — gate เป็นของจริงกี่ตัว

| วันที่ | drill ทั้งหมด | จับได้ | หลุด | gate ที่เป็นของปลอม |
|---|---|---|---|---|
| `<วันนี้>` | — | — | — | — |

**วิธีวัด:**
```bash
git checkout -b drill/$(date +%Y%m%d)
./drills/make-drills.sh
./scripts/gate-drill.sh
```

> **⭐ ตัวเลขนี้มักน่าตกใจกว่าที่คิด**
> ทีมส่วนใหญ่พบว่ามี gate 2–4 ตัวที่ไม่ทำงานเลย
> และไม่มีใครรู้เพราะมันไม่เคยถูกทดสอบ

---

## D. Authz coverage

| วันที่ | endpoint ทั้งหมด | มี spec | ไม่มี spec | % |
|---|---|---|---|---|
| `<วันนี้>` | — | — | — | — |

**วิธีวัด:**
```bash
pnpm test:authz:complete     # จะ fail แล้วลิสต์ endpoint ที่ขาด
```

---

## E. Production invariant

| วันที่ | INV-1 | INV-2 | INV-3 | INV-4 | INV-5 |
|---|---|---|---|---|---|
| `<วันนี้>` | — | — | — | — | — |

*(กรอกจำนวนรายการที่ละเมิดตอนรันครั้งแรก — มักไม่ใช่ 0)*

**⭐ ครั้งแรกที่รัน invariant monitor คือครั้งที่มีค่าที่สุด**
เพราะมันตรวจข้อมูลที่สะสมมาทั้งหมด ไม่ใช่แค่ที่เพิ่งเกิด

---

## F. Human gate

| วันที่ | drill รัน | จับได้ | INCONCLUSIVE | pass rate | blind approve |
|---|---|---|---|---|---|
| `<เดือนที่ 1>` | — | — | — | — | — |

**ตีความคู่กัน:**

| | drill pass สูง | drill pass ต่ำ |
|---|---|---|
| **blind approve สูง** | ✅ ดีที่สุด | 🔴 อันตรายที่สุด — เป็นตรายางแล้ว |
| **blind approve ต่ำ** | 🟡 ปลอดภัยแต่แพง | 🟡 สับสน — presentation อาจแย่ |

---

## G. Performance

| วันที่ | p95 deposit | p95 withdraw | p95 history | coordinated omission |
|---|---|---|---|---|
| `<วันนี้>` | — | — | — | — |

**วิธีวัด:**
```bash
pnpm perf:load                       # ได้ p95
./perf/run-model-compare.sh          # ได้ coordinated omission %
```

---

## H. Bundle

| วันที่ | JS (br) | CSS (br) | Fonts | chunk ใหญ่สุด |
|---|---|---|---|---|
| `<วันนี้>` | — | — | — | — |

```bash
pnpm build && UPDATE_BASELINE=1 node scripts/bundle-budget.mjs
```

---

## I. Oracle strength

| วันที่ | P0–P3 | P4 | P5–P6 | mutation score (money) |
|---|---|---|---|---|
| `<เดือนที่ 2>` | — | — | — | — |

*(ต้องมี mutation testing ก่อน — ทำในเดือนที่ 2)*

---

## J. Knowledge

| วันที่ | กฎทั้งหมด | ACTIVE | RETIRED | consolidation ratio |
|---|---|---|---|---|
| `<วันนี้>` | 57 | 56 | 0 | 0% |

```bash
node scripts/knowledge-gc.mjs
```

**⭐ เป้าคือย้ายกฎจาก ACTIVE → RETIRED**
ความรู้ที่อยู่ในรูปคำแนะนำจะถูกเมินเมื่อไฟล์โตขึ้น
ความรู้ที่กลายเป็น gate ทำงานตลอดไป

---

## หมายเหตุสำหรับการเขียนเนื้อหา

ถ้าจะเอาตัวเลขพวกนี้ไปเผยแพร่ **ต้องใส่คำเตือน**:

> ตัวเลขทั้งหมดมาจากระบบเดียว บริบทเดียว
> เทียบกับระบบอื่นตรงๆ ไม่ได้
> **สิ่งที่ถ่ายทอดได้คือวิธีวัด ไม่ใช่ตัวเลข**
